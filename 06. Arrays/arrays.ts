const superHeroes: string[] = []
// const heroPower: number[] = [] this OR the one below

const heroPower: Array<number> = []

type User = {
    name: string,
    isActive: boolean
}

const allUsers: User[] = []

const MLModels: number[][] = [
    [255, 255, 255],
]




superHeroes.push("Spiderman")
heroPower.push(100)
allUsers.push({name: "Peter Parker", isActive: true})